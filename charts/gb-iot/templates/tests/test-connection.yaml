apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gb-iot.fullname" . }}-test-connection"
  labels:
    {{- include "gb-iot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gb-iot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
