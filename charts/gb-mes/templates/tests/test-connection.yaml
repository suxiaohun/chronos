apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gb-mes.fullname" . }}-test-connection"
  labels:
    {{- include "gb-mes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gb-mes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
