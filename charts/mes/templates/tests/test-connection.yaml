apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mes.fullname" . }}-test-connection"
  labels:
    {{- include "mes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
